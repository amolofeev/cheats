ingressController:
  enabled: true

# админка удобна, когда надо трейсить почему что-то не завелось. / сервиса выплевывает огромный json, в котором сложно разобраться, но снапшот конфигов не плохой
#admin:
#  enabled: true
#  tls:
#    enabled: true
#  http:
#    enabled: true
#  type: ClusterIP

proxy:
  type: LoadBalancer

#env:
#  # судя по исходникам, это не включает, а наоборот отключает не указанные в этом списке. без этого и так все отлично работает.
#  plugins: "bundled,opentelemetry"

envFrom:
  - configMapRef:
      name: kong
